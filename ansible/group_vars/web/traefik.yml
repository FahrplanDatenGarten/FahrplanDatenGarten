---
traefik_tls_letsencrypt_challenges_http:
  enable: yes
  email: "letsencrypt@fahrplandatengarten.de"
  name: "le_http" # Name of the certificate resolver
  entryPoint: "http"
  storage: "{{ traefik_base_path }}/acme_http.json"
traefik_default_cert_resovler: "le_http"
traefik_middlewares_dashboard_authentication:
  enable: yes
  name: "dashboard_authentication"
  realm: "Traefik Dashboard"
  remove_header: yes
  users:
    - "em0lar:$apr1$zwtlSO.m$gTjvEeWDKLpJQtSt.MN281" # Password can be generated by htpasswd tool

